# raspi-config

> Un'interfaccia GUI terminale `ncurses` per configurare un Raspberry Pi.
> Maggiori informazioni: <https://www.raspberrypi.com/documentation/computers/configuration.html>.

- Avvia `raspi-config`:

`sudo raspi-config`
