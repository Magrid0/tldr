# systemd-confext

> Questo comando è un alias per `systemd-sysext`.
> Segue lo stesso principio di `systemd-sysext`, ma invece di agire su `/usr` e `/opt`, `confext` estende solo `/etc`.

- Consulta la documentazione del comando originale:

`tldr systemd-sysext`
