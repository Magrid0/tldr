# yum

> Questo comando è un alias per `dnf` sulle distribuzioni moderne.
> Per distribuzioni più vecchie (es. CentOS 7), `yum` è un programma distinto da `dnf`. Alcuni sottocomandi e opzioni potrebbero essere diversi.

- Visualizza la documentazione per il comando originale:

`tldr dnf`
