# rpm2cpio

> Converte un pacchetto RPM in un archivio `cpio`.
> Maggiori informazioni: <http://ftp.rpm.org/max-rpm/s1-rpm-miscellania-rpm2cpio.html>.

- Converte un pacchetto RPM in un archivio `cpio` e lo salva come `file.cpio` nella directory corrente:

`rpm2cpio {{path/to/file.rpm}}`
