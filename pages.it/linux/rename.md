# rename

> `rename` può riferirsi a più comandi con lo stesso nome.

- Mostra la documentazione per la versione Perl:

`tldr {{[-p|--platform]}} common rename`

- Mostra la documentazione per la versione `util-linux`:

`tldr rename.util`
