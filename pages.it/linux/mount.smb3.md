# mount.smb3

> Questo comando è un alias per `mount.cifs`.
> Nota: per versioni SMB precedenti alla 3 devi usare `mount.cifs`.
> Maggiori informazioni: <https://manned.org/mount.cifs>.

- Visualizza la documentazione per il comando originale:

`tldr mount.cifs`
