# mkfs.minix

> Crea un filesystem Minix all'interno di una partizione.
> Maggiori informazioni: <https://manned.org/mkfs.minix>.

- Crea un filesystem Minix all'interno della partizione Y sul dispositivo X:

`sudo mkfs.minix {{/dev/sdXY}}`
