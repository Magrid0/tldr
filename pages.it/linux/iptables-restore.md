# iptables-restore

> Ripristina la configurazione IPv4 di `iptables`.
> Usa `ip6tables-restore` per fare lo stesso per IPv6.
> Maggiori informazioni: <https://manned.org/iptables-restore>.

- Ripristina la configurazione di `iptables` da un file:

`sudo iptables-restore {{path/to/file}}`
