# zforce

> Aggiunge un'estensione `.gz` ai file compressi con `gzip`.
> Maggiori informazioni: <https://manned.org/zforce>.

- Aggiungi un'estensione `.gz` ai file Gzip forniti (Nota: gli altri file vengono ignorati):

`zforce {{path/to/file1 path/to/file2 ...}}`
