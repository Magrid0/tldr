# insmod

> Carica dinamicamente moduli nel kernel Linux.
> Vedi anche: `kmod`.
> Maggiori informazioni: <https://manned.org/insmod>.

- Inserisce un modulo del kernel nel kernel Linux:

`sudo insmod {{path/to/module.ko}}`
