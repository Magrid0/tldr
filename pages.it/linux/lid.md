# lid

> `lid` può riferirsi a più comandi con lo stesso nome.

- Visualizza la documentazione per lo strumento `libuser`:

`tldr lid.libuser`

- Visualizza la documentazione per lo strumento `idutils`:

`tldr lid.idutils`
